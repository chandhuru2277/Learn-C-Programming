#include <stdio.h>

int main(void)
{
    // STRING LITERAL : IT STORE'S THE STRING VALUE ON THE RAM MEMORY THEN RETURN THE ADDRESSS OF THE STRING
    // IT'S NOT MUTABLE
    // DATA_TYPE *VARIABLE_NAME= "STRING";
    char *str = "william";
    printf("\nstring: %s\n", str);

    return 0;
}